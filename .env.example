# Sviluppo (Vite dev server)
HOST=127.0.0.1
PORT=3000

# Anteprima (vite preview)
PREVIEW_HOST=127.0.0.1
PREVIEW_PORT=8080

# Note:
# - Imposta HOST a 127.0.0.1 se l'antivirus blocca localhost o porte predefinite
# - Puoi cambiare le porte se risultano occupate o filtrate
