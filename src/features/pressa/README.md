# Uso rapido
1) Inserisci `<PressSelector />` dove configuri i parametri.
2) Ricevi `specs` (dati macchina) + `derived` (suggerimenti adattivi) in `onPressSelected`.
3) Passa questi valori al tuo motore calcoli (punto di commutazione, profilo velocità, post-pressione, clamp).